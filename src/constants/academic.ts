/**
 * EduSphere — constants/academic.ts
 * -----------------------------------------------------------------------
 * Programme and department are real backend data now (see courseService's
 * getProgrammes/getDepartments) — level is the one academic field with no
 * catalog behind it, since KNUST's levels really are just this fixed set.
 * -----------------------------------------------------------------------
 */

export const LEVEL_OPTIONS = ['Level 100', 'Level 200', 'Level 300', 'Level 400'];
